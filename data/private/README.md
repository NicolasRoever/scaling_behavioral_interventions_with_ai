# Withheld inputs

Raw interview transcripts, participant-level text excerpts, screenshot images, rendered model payloads and model explanations are not distributed. The source methods are under `code/private/`; running those upstream methods requires the original restricted data. Public numeric results can be rebuilt using `code/python/run_public.py` and `code/stata/02_run_exhibits.do`.
